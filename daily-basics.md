# Daily Basics

这份清单用于 EnvMonitor 每天约 2 小时的开发节奏。当天只选一个可验证的小目标，例如“解析 CRC 错帧后能恢复”，不要同时跨协议、通信和 UI 三层改动。

## 开工（10 分钟）

1. 拉取当前分支的变更，确认没有未理解的本地修改。
2. 运行现有测试；若失败，先记录失败用例和原因，不把它混入当天的新功能。
3. 在 README 开发日志写下今天的目标、验收条件和第一条测试用例。
4. 对照协议命令表确认字段长度、字节序、缩放比例和错误响应规则。

## 实现循环（80 分钟）

1. 先添加或修改一个最小测试，覆盖正常路径与一个边界或失败路径。
2. 实现最小代码使测试通过。协议层不引用 TCP 或 WPF；通信层不引用 WPF。
3. 立即运行受影响的测试项目。失败时先修当前切片，不扩展到下一个功能。
4. 对网络改动额外验证：半包、粘包、错误 CRC、超时、断线后未决请求是否被清理。
5. 对 UI 改动额外验证：绑定错误、后台事件是否切回 Dispatcher、窗口关闭后计时器和连接是否停止。

## 本地演示（20 分钟）

1. 启动模拟器，再启动 App，完成一次连接、读取、继电器操作和断开。
2. 当天涉及通信时，使用对应 FaultOptions 或命令行故障参数复现一次异常场景。
3. 记录实际结果；未完成项写 TODO，附上复现条件和下一步，而不是留下无说明的临时代码。

## 收工（10 分钟）

1. 运行 `dotnet test` 和 `dotnet build`，确认通过或明确记录既有失败。
2. 检查 `git diff`，删除调试输出、无用注释和与当天目标无关的修改。
3. 更新 README 开发日志：完成内容、验证命令、遇到的问题、明天第一件事。
4. 使用清晰的提交信息提交，例如 `feat: add deterministic CRC fault injection`。

## 每日完成定义

当天任务只有同时满足以下条件才算完成：代码已提交；至少一个自动化测试覆盖关键行为；手动演示或集成测试已执行；异常路径的预期结果已记录。
